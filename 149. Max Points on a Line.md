

Problem :

Given an array of points where points[i] = [xi, yi] represents a point on the X-Y plane, return the maximum number of points that lie on the same straight line.

 

Example 1:

<img width="510" height="500" alt="image" src="https://github.com/user-attachments/assets/8b3cba8a-0b1b-4366-8d50-f1163b41b60f" />

Input: points = [[1,1],[2,2],[3,3]]
Output: 3
Example 2:

<img width="510" height="500" alt="image" src="https://github.com/user-attachments/assets/77a64d19-c0aa-4e57-87b8-be311b577c3e" />

Input: points = [[1,1],[3,2],[5,3],[4,1],[2,3],[1,4]]
Output: 4
 

Constraints:

1 <= points.length <= 300
points[i].length == 2
-104 <= xi, yi <= 104
All the points are unique.
_______________________________________________________________________________________________________________________________________________________________

Explanation : 

<img width="1690" height="665" alt="image" src="https://github.com/user-attachments/assets/2850803b-c72b-4254-9844-ccb854ec5463" />
<img width="1492" height="1265" alt="image" src="https://github.com/user-attachments/assets/2b0a3e4c-b8b9-43a4-9399-830180c9b2c4" />

_______________________________________________________________________________________________________________________________________________________________

Solution :

```java
class Solution {
    int gcd(int a,int b){
        if(a==0) return b;
        return gcd(b%a,a);
    }
    public int maxPoints(int[][] points) {
        int max = 0;
        for(int i=0;i<points.length;i++){
            Map<Long,Integer> map = new HashMap<>();
            int vertical = 0;
            int duplicate = 0;
            for(int j=i+1;j<points.length;j++){
                int dx = points[j][0] - points[i][0];
                int dy = points[j][1] - points[i][1];
                if(dx==0 && dy == 0){
                    duplicate++;
                    continue;
                }
                if(dx == 0) vertical++;
                else {
                    int gcd = gcd(dx,dy);
                    dx /= gcd;
                    dy /= gcd;
                    if(dx < 0){
                        dx = -dx;
                        dy = -dy;
                    }
                    long key = ( (long)dy << 32 ) | (dx & 0xffffffffL);
                    map.merge(key,1,Integer::sum);
                }
            }
            int cmax = vertical;
            for(int val : map.values()){
                cmax = Math.max(cmax,val);
            }
            max = Math.max(max,cmax+duplicate);
        }
        return max+1;
    }
}
```

